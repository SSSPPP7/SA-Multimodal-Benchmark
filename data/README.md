# Data

Place the processed training file here:

```text
data/processed/mdf_sacn_samples.npz
```

Required arrays:

```text
eeg, em, subject_id, probe_id, y_rec, y_pred, mask_pred
```

Optional arrays:

```text
mask_rec, time_sec, quality_eeg, quality_em
```
